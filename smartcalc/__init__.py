"""
SmartCalc - Une calculatrice scientifique avancée

Ce package contient les modules pour la calculatrice scientifique SmartCalc,
y compris l'interface graphique, les modèles de calcul et les contrôleurs.
"""

__version__ = "0.1.0"

# Les points d'entrée (main) sont gérés au niveau du dépôt (main.py).
# Aucun re-export depuis le package pour éviter des dépendances circulaires
# ou des erreurs d'import lors de l'analyse statique.
